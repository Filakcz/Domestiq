Models
-Pouze data

Services
-Veci mimo ui
-Algoritmus A*, parse GPX

ViewModels
-Mozek ui
-Reakce na kliknuti

Views
-Jen UI (XAML)

Styles
-Styl UI

------------------------------------------------------------------------
Kompilace:
Windows:
dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true
Linux:
dotnet publish -c Release -r linux-x64 --self-contained true -p:PublishSingleFile=true
